# # OOP is one of the most effective approaches to writing software. 
# # In OOP, we write CLASSES that represent real-world things and situations
# # you create OBJECTS  bases on these classes.

# # When you write a class, you define the general behaviour that a whole category of objects can have.

# # INSTANTIATION - Making an object from a class
# # You work with instances of a class.

# # Specify the kind of information that can be stored in instances and
# # define actions that can be taken with these instances.

# # similar classes can share common functionality, to perform more with less code.

# # You'll store classes in modules and import classes written by other programmers into your own program files.
# # Knowing the logic behind classes will train you to think logically, 
# # so you can write programs that effectively address almost any problem you encounter.

# # classes help you take on increasingly complex challenges.


# '''CREATING AND USING A CLASS'''

# # you can model almost anything using classes.
# # classes represent the most common information of an object

# # After the class is written, we will use it to make individual instances, 
# # each of which represents one specific dog.

# '''CREATING THE DOG CLASS'''

# class Dog:                                
#     """A simple attempt to model a dog."""

#     def __init__(self, name, age):
#         """Initialize name and age attributes."""
#         self.name = name
#         self.age = age
    
#     def sit(self):
#         """Simulate a dog sitting in response to a commond."""
#         print(f"{self.name} is now sitting.")
    
#     def roll_over(self):
#         """Simulate rolling over in response to a command."""
#         print(f"{self.name} rolled over!")
    

# # Capitalized names refer to classes in Python
# # No parantheses because we are creating this class from scratch.

# # A function thats part of a class is a method. e.g __init__() method.

# # __init__ method is a special method that Python runs automatically
# #  whenever we create a new instance based on the Dog class

# # __  __ prevent Python's default method names from conflicting with your method names.
# # It has three parameters: self, name and age.
# # 'self' must be included in the definition because when python calls this method later (to create an instance of Dog)
# # the method call will automatically pass the 'self' argument

# # self refers to the instance itself.
# # whenever, we want to make an instance from the Dog class, 
# # we'll provide values for only the last two parameters (name, age)

# # Any variable prefixed with 'self' is available to every method in the class.
# # Variables that are accessible through instances are called 'ATTRIBUTES'.


# '''MAKING AN INSTANCE FROM A CLASS'''

# # Dog class is a set of instructions that tells Python how to make individual instances representing specific dogs.

# my_dog = Dog('Willie', 6)       # my_dog (lowercase name) refers to 'single instance' created from a class

# # The __init__method creates an instance representing this particular dog and 
# # sets the name and age attributes using the values we provided.


# print(f"My dog's name is {my_dog.name}.")
# print(f"My dog's  is {my_dog.age} years old.")

# '''ACCESSING ATTRIBUTES'''

# # DOT NOTATION  is used to access the attributes of an instance.

# my_dog.name


# '''CALLING METHODS'''

# # After we create an instance, we can use Dot notation to CALL ANY METHOD defined in Dog.
# print(f"\n")
# my_dog.sit()            # calling sit method
# my_dog.roll_over()      # calling roll_over method

# # Descriptive names like name, age, sit() and roll_over() help in easy inference of code.


# '''CREATING MULTIPLE INSTANCES'''

# your_dog = Dog('Scooby', 3)

# print(f"\nMy dog's name is {my_dog.name}.")
# print(f"{my_dog.name} is {my_dog.age} years old.")
# my_dog.sit()

# print(f"\nYour dog's name is {your_dog.name}.")
# print(f"{your_dog.name} is {your_dog.age} years old.")
# your_dog.roll_over()

# # You can make as many instances from one class as you need, 
# # as long as you give each instance a unique variable name or 
# # it occupies a unique spot in a list or dictionary.


# '''TRY IT YOURSELF'''

# # 9.1.  RESTAURANT

# class Restaurant:
#     def __init__(self, restaurant_name, cuisine_type):
#         self.restaurant_name = restaurant_name
#         self.cuisine_type = cuisine_type

#     def describe_restaurant(self):
#         print(f"\nThe restaurant's name is {self.restaurant_name.title()}.")
#         print(f"{self.restaurant_name.title()} hosts {self.cuisine_type.title()} cuisine.")
    
#     def open_restaurant(self):
#         print(f"\nThe {self.restaurant_name} restaurant is OPEN.")

# restaurant = Restaurant('Bakasurra', 'south indian')
# restaurant.describe_restaurant()
# restaurant.open_restaurant()


# # 9.2.  THREE RESTAURANTS

# restaurant1 = Restaurant('f3 cafe and bistro', 'italian')
# restaurant1.describe_restaurant()

# restaurant2 = Restaurant('shang palace', 'chinese')
# restaurant2.describe_restaurant()

# # 9.3.  USERS

# class User:
#     def __init__(self, first_name, last_name, age, occupation):
#         self.first_name = first_name
#         self.last_name = last_name
#         self.age = age
#         self.occupation = occupation
    
#     def describe_user(self):
#         user_info = f"{self.first_name.title()} {self.last_name.title()}, {self.age}, {self.occupation.title()}"
#         print(f"Your info: {user_info}")
    
#     def greet_user(self):
#         full_name = f"{self.first_name} {self.last_name}"
#         print(f"\nHello, {full_name.title()}!")

# user1 = User('johnny', 'dep', 51, 'actor')
# user1.greet_user()
# user1.describe_user()

# user2 = User('virat', 'kohli', 38, 'cricketer')
# user2.greet_user()
# user2.describe_user()

# user3 = User('abdul', 'kalam', 75, 'scientist')
# user3.greet_user()
# user3.describe_user()



# '''WORKING WITH CLASSES AND INSTANCES'''

# # Once you write a class, you'll spend most of your time working with instances created from that class.
# # First task - modify the attributes directly or write methods that update attributes in specific ways

# '''The Car class'''

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make} {self.model}"
#         return long_name.title()
    
# my_new_car = Car('mercedes', 'CLA350', '2026')
# print(my_new_car.get_descriptive_name())

# # Let's add an attribute that changes over time. We'll add an attribute that stores the car's overall mileage.

# '''SETTING A DEFAULT VALUE FOR AN ATTRIBUTE'''

# # When an instance is created, attributes can be defined without being passed in as parameters.

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year
#         self.odometer_reading = 0       # Python created a new attribute called odometer_reading and sets its initial value to 0.

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make} {self.model}"
#         return long_name.title()
    
#     def read_odometer(self):
#         "Print a statement showing the car's mileage."
#         print(f"This car has {self.odometer_reading} miles on it.")
    
# my_new_car = Car('mercedes', 'CLA350', '2026')
# print(my_new_car.get_descriptive_name())
# my_new_car.read_odometer()


# '''MODIFYING ATTRIBUTE VALUES'''

# # Attribute values can be changed in three ways.
# #1 Directly through an instance
# #2 Set the value through a method
# #3 Increment the value (add a certain amount to it) through a method.

# '''#1 - MODIFYING AN ATTRIBUTE'S VALUE DIRECTLY THROUGH AN INSTANCE'''

# my_new_car.odometer_reading = 23
# my_new_car.read_odometer()

# '''#2 - MODIFYING AN ATTRIBUTE'S VALUE THROUGH A METHOD'''

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year
#         self.odometer_reading = 0       # Python created a new attribute called odometer_reading and sets its initial value to 0.

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make} {self.model}"
#         return long_name.title()
    
#     def read_odometer(self):
#         "Print a statement showing the car's mileage."
#         print(f"This car has {self.odometer_reading} miles on it.")
    
#     def update_odometer(self, mileage):                         # Addition of update_odometer
#         """Set the odometer reading to the given value."""
#         self.odometer_reading = mileage
    
# my_new_car = Car('mercedes', 'CLA350', '2026')
# print(my_new_car.get_descriptive_name())

# my_new_car.update_odometer(25)
# my_new_car.read_odometer()


# # add a little logic to make sure no one tries to roll back the odometer reading.

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year
#         self.odometer_reading = 0       # Python created a new attribute called odometer_reading and sets its initial value to 0.

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make} {self.model}"
#         return long_name.title()
    
#     def read_odometer(self):
#         "Print a statement showing the car's mileage."
#         print(f"This car has {self.odometer_reading} miles on it.")
    
#     def update_odometer(self, mileage):                         # Addition of update_odometer
#         """Set the odometer reading to the given value.
#         Reject the change if it attempts to roll back the odometer."""
#         if mileage >= self.odometer_reading:
#             self.odometer_reading = mileage
#         else:
#             print("You can't roll back on odometer.")
    
# my_new_car = Car('mercedes', 'CLA350', '2026')
# print(my_new_car.get_descriptive_name())

# my_new_car.update_odometer(15)
# my_new_car.read_odometer()


# '''INCREMENTING AN ATTRIBUTE'S VALUE THROUGH A METHOD'''

# # We might want to increase an attribute's value by a certain amount, rather than set an entirely new value.

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year
#         self.odometer_reading = 0       # Python created a new attribute called odometer_reading and sets its initial value to 0.

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make} {self.model}"
#         return long_name.title()
    
#     def read_odometer(self):
#         "Print a statement showing the car's mileage."
#         print(f"This car has {self.odometer_reading} miles on it.")
    
#     def update_odometer(self, mileage):                         # Addition of update_odometer
#         """Set the odometer reading to the given value.
#         Reject the change if it attempts to roll back the odometer."""
#         if mileage >= self.odometer_reading:
#             self.odometer_reading = mileage
#         else:
#             print("You can't roll back on odometer.")
    
#     def increment_odometer(self, miles):
#         """Add the given amount to the odometer reading."""
#         self.odometer_reading += miles

# my_used_car = Car('mercedes', 'CLA350', '2026')
# print(my_used_car.get_descriptive_name())

# my_used_car.update_odometer(25_600)
# my_used_car.read_odometer()

# my_used_car.increment_odometer(100)
# my_used_car.read_odometer()


# '''TRY IT YOURSELF'''

# # 9.4 NUMBER SERVED

# class Restaurant:
#     def __init__(self, restaurant_name, cuisine_type):
#         self.restaurant_name = restaurant_name
#         self.cuisine_type = cuisine_type
#         self.number_served = 0

#     def describe_restaurant(self):
#         print(f"\nThe restaurant's name is {self.restaurant_name.title()}.")
#         print(f"{self.restaurant_name.title()} hosts {self.cuisine_type.title()} cuisine.")
#         print(f"The number of customers the restaurant has served so far is {self.number_served}.")
#         print(f"The restaurant served an additional {self.last_increment} customers, totalling to {self.total_served}.")   # added due to increment
    
#     def open_restaurant(self):
#         print(f"The {self.restaurant_name} restaurant is OPEN.")

#     def set_number_served(self, number_served):                         # Addition of update_odometer
#         """Sets the number of customers served to new value."""
#         self.number_served = number_served
    
#     def increment_number_served (self, additional_increment):
#         self.total_served = self.number_served + additional_increment
#         self.last_increment =  additional_increment

# # First object
# restaurant = Restaurant('Bakasurra', 'south indian')
# restaurant.open_restaurant()
# restaurant.set_number_served(15)    # Upodated after printing the description, so not printed
# restaurant.increment_number_served(35)
# restaurant.describe_restaurant()


# # # Second object
# restaurant1 = Restaurant('Chings', 'chinese')
# restaurant1.set_number_served(25)
# restaurant1.increment_number_served(75)
# restaurant1.describe_restaurant()


# # 9.5.  LOGIN ATTEMPTS

# class User:
#     def __init__(self, first_name, last_name, age, occupation):
#         self.first_name = first_name
#         self.last_name = last_name
#         self.age = age
#         self.occupation = occupation
#         self.login_attempts = 0
    
#     def describe_user(self):
#         user_info = f"Name: {self.first_name.title()} {self.last_name.title()}, \n\tAge: {self.age}, \n\tOccupation: {self.occupation.title()}, \n\tlogin attempts: {self.login_attempts}"
#         print(f"\nUser info: {user_info}")
    
#     def greet_user(self):
#         full_name = f"{self.first_name} {self.last_name}"
#         print(f"\nHello, {full_name.title()}!")
    
#     def increment_login_attempts(self):
#         self.login_attempts += 1
    
#     def reset_login_attempts(self):
#         self.login_attempts = 0

# print("Results after login attempts:")

# user1 = User('johnny', 'dep', 51, 'actor')
# user1.increment_login_attempts()
# user1.describe_user()

# user2 = User('virat', 'kohli', 38, 'cricketer')
# user2.increment_login_attempts()
# user2.increment_login_attempts()
# user2.describe_user()

# user3 = User('abdul', 'kalam', 75, 'scientist')
# user3.increment_login_attempts()
# user3.increment_login_attempts()
# user3.increment_login_attempts()
# user3.describe_user()

# # Reset login attempts

# print("\nResults after resetting attempts:")
# user1.reset_login_attempts()
# user1.describe_user()

# user2.reset_login_attempts()
# user2.describe_user()

# user3.reset_login_attempts()
# user3.describe_user()


# '''INHERITANCE'''

# # Inheritance is used to write a specialized version of another class (child class).
# # It takes on the attributes and methods of the first class (parent class).
# # The child class can inherit any or all of the attributes and methods of its parent class, but,
# # It's also free to define new attributes and methods of its own.

# '__init__() Method for a Child Class' 

# # Model an ElectricCar sub-class on the Car parent-class and call  the __init__() method from the parent class.
# # The parent class must be part of the current file and must appear before the child class in the file.
# # Parent class must be included in the parentheses.

# # super() function is a special function that allows to call a method from the parent class.
# # The name 'super' comes from the convention of calling the parent class a 'superclass' and the child class a 'subclass'

# class Car:
#     """A simple attempt to represent a car"""

#     def __init__(self, make, model, year):
#         """Initialise attributes to describe a car."""
#         self.make = make
#         self.model = model
#         self.year = year
#         self.odometer_reading = 0       # Python created a new attribute called odometer_reading and sets its initial value to 0.

#     def get_descriptive_name(self):
#         """Return a neatly formatted descriptive name."""
#         long_name = f"{self.year} {self.make.title()} {self.model.title()}"
#         return long_name.title()
    
#     def read_odometer(self):
#         "Print a statement showing the car's mileage."
#         print(f"This car has {self.odometer_reading} miles on it.")
    
#     def update_odometer(self, mileage):                         # Addition of update_odometer
#         """Set the odometer reading to the given value.
#         Reject the change if it attempts to roll back the odometer."""
#         if mileage >= self.odometer_reading:
#             self.odometer_reading = mileage
#         else:
#             print("You can't roll back on odometer.")
    
#     def increment_odometer(self, miles):
#         """Add the given amount to the odometer reading."""
#         self.odometer_reading += miles
    

# class ElectricCar(Car):
#     """Represents aspects of a car, specific to electric vehicles."""
#     def __init__(self, make, model, year):
#         super().__init__(make, model, year)

# my_leaf = ElectricCar('nissan', 'leaf', 2024)
# print(my_leaf.get_descriptive_name())


# class ElectricCar1(Car):
#     """Initializes attributes of parent class."""
#     """Then initialize attributes specific to an electric car."""
#     def __init__(self, make, model, year):
#         super().__init__(make, model, year)
#         self.battery_size = 40                  # There is no limit on how much you can specialize (adding attributes) the ElectricCar class to whatever degree of accuracy you need.

#     def describe_battery(self):
#         """Print a statement describing the battery size."""
#         print(f"This car has a {self.battery_size} kWh battery.")

# my_leaf = ElectricCar1('nissan', 'leaf', 2024)
# print(my_leaf.get_descriptive_name())
# my_leaf.describe_battery()


# '''Overriding Methods from the Parent Class'''

# # You can override any method from the parent class that doesn't fit what you're trying to model with the child class.
# # You define a method in the child class with the same name as the method you want to override in the parent class.

# class ElectricCar(Car):

#     def fill_gas_tank(self):
#         """Electric cars don't have gas tanks."""
#         print("This car does not have a gas tank coz its electric!")

# # The above code will run in case if any one calls the fill_gas_tank method within ElectricCar class, provided,
# # same fill_gas_tank method is also available in Car superclass.



# '''INSTANCES AS ATTRIBUTES'''

# # While coding for real world problem, we will have a growing list of attributes and methods and 
# # that your files are becoming lengthy.
# # In these situations, you might recognize that part of one class can be written as separate class.
# # You can break a large class into smaller classes that work together; this approach is called 'COMPOSITION'.

# # Let's create a separate class called Battery. Then we can use a Battery instance as as an attribute in the ElectricCar class.

# class Battery:
#     """A simple attempt to model a battery for an electric car."""

#     def __init__(self, battery_size = 40):
#         """Initialize the battery's attributes."""
#         self.battery_size = battery_size
    
#     def describe_battery(self):
#         """Print a statement describing the battery size."""
#         print(f"The car has a {self.battery_size} kWh battery.")
    
#     def get_range(self):
#         """Print a statement about the range this battery provides."""
#         if self.battery_size == 40:
#             range = 150
#         elif self.battery_size == 65:
#             range = 225
        
#         print(f"This car has a range of {range} miles on a full charge.")
    
#     def upgrade_battery(self):
#         if self.battery_size != 65:
#             self.battery_size = 65


# class ElectricCar2(Car):
#     """Initializes attributes of parent class."""
#     """Then initialize attributes specific to an electric car."""
#     def __init__(self, make, model, year):
#         super().__init__(make, model, year)
#         self.battery = Battery()                  # Calling the instance from the class Battery

#     def describe_battery(self):
#         """Print a statement describing the battery size."""
#         print(f"This car has a {self.battery_size} kWh battery.")

# my_leaf = ElectricCar2('nissan', 'leaf', 2024)
# print(my_leaf.get_descriptive_name())
# my_leaf.battery.describe_battery()
# my_leaf.battery.get_range()


# '''MODELING REAL WORLD OBJECTS'''

# # What if we're describing a manufacturer's entire life of cars,
# # we probably want to move get_range() to the ElectricCar class.
# # There are often no right or wrong approaches to modeling real-world situations.
# # Some approaches are more efficient than other, 
# # but it takes practice to find the most efficient representations.


# '''TRY IT YOURSELF'''

# # 9.6.  ICE CREAM STAND

# class Restaurant:
#     def __init__(self, restaurant_name, cuisine_type):
#         self.restaurant_name = restaurant_name
#         self.cuisine_type = cuisine_type
#         self.number_served = 0

#     def describe_restaurant(self):
#         print(f"\nThe restaurant's name is {self.restaurant_name.title()}.")
#         print(f"{self.restaurant_name.title()} serves delicious {self.cuisine_type.title()}.")
      
#     def open_restaurant(self):
#         print(f"The {self.restaurant_name} is OPEN. ")

#     def set_number_served(self, number_served):                         # Addition of update_odometer
#         """Sets the number of customers served to new value."""
#         self.number_served = number_served
    
#     def increment_number_served (self, additional_increment):
#         self.total_served = self.number_served + additional_increment
#         self.last_increment =  additional_increment


# class IceCreamStand(Restaurant):
#     """Represents aspects of Ice Cream Stand"""

#     def __init__(self, restaurant_name, cuisine_type):
#         """initializing an IceCreamStand"""
#         super().__init__(restaurant_name, cuisine_type)
#         self.flavours = []

#     def icecream_flavours(self):
#         """Provides the list of flavours available in IceCreamStand."""
#         print(f"The {self.restaurant_name} serves the following {self.cuisine_type}'s:")
#         for flavour in self.flavours:
#             print(f"- {flavour.title()}")


# masquati = IceCreamStand('masqati', 'icecream')
# masquati.flavours = ['vanilla', 'black currant', 'strawberry']
# masquati.describe_restaurant()
# masquati.icecream_flavours()


# # 9.7.  ADMIN

# class User:
#     def __init__(self, first_name, last_name, age, occupation):
#         self.first_name = first_name
#         self.last_name = last_name
#         self.age = age
#         self.occupation = occupation
#         self.login_attempts = 0
    
#     def describe_user(self):
#         user_info = f"Name: {self.first_name.title()} {self.last_name.title()}, \n\tAge: {self.age}, \n\tOccupation: {self.occupation.title()}, \n\tlogin attempts: {self.login_attempts}"
#         print(f"\nUser info: {user_info}")
    
#     def greet_user(self):
#         full_name = f"{self.first_name} {self.last_name}"
#         print(f"\nHello, {full_name.title()}!")
    
#     def increment_login_attempts(self):
#         self.login_attempts += 1
    
#     def reset_login_attempts(self):
#         self.login_attempts = 0


# class Admin(User):
#     """Represents the privileges provided to Admin."""
#     def __init__(self, first_name, last_name, age, occupation):
#         super().__init__(first_name, last_name, age, occupation)
#         self.privileges = []
    
#     def show_privileges(self):
#         """Displays the set of privileges provided to the Administrator."""
#         print("The adminstrator has the following privileges:")
#         for privilege in self.privileges:
#             print(f"- {privilege.title()}")

# manager = Admin('amit', 'shah', 38, 'Manager')
# manager.describe_user()
# manager.privileges = ["can add post", 'can delete post', 'can ban user']
# manager.show_privileges()


# # 9.8.  PRIVILEGES

# class Admin(User):
#     """Represents the privileges provided to Admin."""
#     def __init__(self, first_name, last_name, age, occupation):
#         super().__init__(first_name, last_name, age, occupation)
#         self.privileges = Privileges()


# class Privileges:
#     """A class to store admin's privileges"""

#     def __init__(self, privileges = None):
#         if privileges == None:
#             print("This user has no privileges.")
#         self.privileges_list = privileges
    
#     def show_privileges(self):
#         """Displays the set of privileges provided to the Administrator."""
#         print("The adminstrator has the following privileges:")
#         for privilege in self.privileges_list:
#             print(f"- {privilege.title()}")

# manager = Admin('amit', 'shah', 38, 'Manager')
# manager.privileges.privileges_list = ["can add post", 'can delete post', 'can ban user']
# manager.privileges.show_privileges()

# sales_person = Admin('salman', 'khan', 48, 'sales person')
# sales_person.privileges.privileges_list = []
# # sales_person.privileges.show_privileges()


# '''
# 🧠 Key Takeaway

# 👉 Don’t overwrite objects with different types
# 👉 Modify attributes inside the object instead
# '''


# # 9.9.  Battery upgrade

# class ElectricCar2(Car):
#     """Initializes attributes of parent class."""
#     """Then initialize attributes specific to an electric car."""
#     def __init__(self, make, model, year):
#         super().__init__(make, model, year)
#         self.battery = Battery()                  # Calling the instance from the class Battery

#     def describe_battery(self):
#         """Print a statement describing the battery size."""
#         print(f"This car has a {self.battery_size} kWh battery.")
    

# my_leaf = ElectricCar2('nissan', 'leaf', 2024)
# print(my_leaf.get_descriptive_name())
# my_leaf.battery.describe_battery()
# my_leaf.battery.get_range()
# print("If the current battery size != 65, Then the battery will be upgraded to 65 kWh.")
# my_leaf.battery.upgrade_battery()
# my_leaf.battery.describe_battery()
# my_leaf.battery.get_range()


# '''IMPORTING CLASSES'''

# # Your files can get long, even when you use inheritance and composition properly.
# # In keeping with th overall philosophy of Python, you'll want to keep your files as uncluttered as possible.
# # Python let's you to store classes in modules and then import the classes you need into your main program.


# '''Importing a Single Class'''

# from car import Car

# my_new_car = Car('audi', 'a4', 2024)
# print(my_new_car.get_descriptive_name())

# my_new_car.odometer_reading = 23
# my_new_car.read_odometer()

# # Importing classes is an efficient way to program.
# # Its adviced to move the class to a module and import the module.
# # you still get all the same functionality, 
# # but you keep your main program file clean and easy to read.
# # You also store most of the logic in separate files.
# # Importing classes helps to focus on the higher level logic of your main program.

# '''Storing multiple classes in a Module'''

# # classes Battery and ElectricCar both help represent cars, so let's add them to the module car.py

# 'Check car.py and my_electric_car.py'

# """Importing multiple Classes from a Module"""

# # importing two classes in the same file

# from car import Car, ElectricCar

# my_mustang = Car('ford', 'mustang', 2025)
# print(my_mustang.get_descriptive_name())

# my_leaf = ElectricCar('nissan', 'leaf', 2025)
# print(my_leaf.get_descriptive_name())


# '''Importing an entire Module'''

# import car

# my_tesla = Car('tesla', 'model-s', 2026)
# print(my_tesla.get_descriptive_name())

# my_leaf1 = ElectricCar('nissan', 'leaf1', 2026)
# print(my_leaf1.get_descriptive_name())


# '''Importing all classes from a Module'''

# from car import *

# # This method is not recommended.
# # Reason - With this approach it’s unclear which classes you’re using from the module.
# # This approach can also lead to confusion with names in the file. 
# # Sometimes importing a class with the same name as something else in the program,
# # can create errors that are hard to diagnose.

# '''Importing a Module into a Module'''

# # When you store your classes in several modules, 
# # you may find that a class in one module depends on a class in another module.
# # When this happens, you can import the required class into the first module.

# '''
# We have added Battery and ElectricCar classes in my_electric_car.py and
# removed these both classes from car.py
# we created a new file and imported both modules (Car and ElectricCar)
# '''

"""Using ALIASES"""

from my_electric_car import ElectricCar as EC
myleaf = EC('nissan', 'leaf', 2026)
print(myleaf.get_descriptive_name()) 

import my_electric_car as ec
myleaf1 = ec.ElectricCar('nissan', 'leaf1', 2025)
print(myleaf1.get_descriptive_name())

